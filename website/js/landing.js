/*

function getMainlanding() {
    const doc = document.getElementById("mainLanding");
    doc.innerHTML = '';
    return doc;
}

function createCard() {
    const doc = getMainlanding();
   
    const card = document.createElement("div");
    card.className = "card";
    card.id = "card";
    doc.appendChild(card);
    return card;
}

function inputCardSignUp(){
    const card = createCard();
    const form = document.createElement("form");
    form.className = "form";
    form.id = "form";
    const inputMail = document.createElement("input");
    inputMail.className = "mail";
    inputMail.id = "mail";
    inputMail.type = "email";
    inputMail.placeholder = "Email";
    form.appendChild(inputMail);
    card.appendChild(form);
    return form;
}

function inputCardSignIn(){
    const card = createCard();
    const form = document.createElement("form");
    form.className = "form";
    form.id = "form";
    card.appendChild(form);
    return form;
}
*/